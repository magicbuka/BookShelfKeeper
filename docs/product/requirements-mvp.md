# BookShelf Keeper — Requirements MVP

**Версия:** 0.3.0 (MVP)  
**Статус:** Draft  
**Дата:** 2025-12-18  

## 1. Цель и границы MVP

### 1.1. Цель продукта

**BookShelf Keeper** — Android-приложение для владельцев домашних библиотек, позволяющее:

- быстро заносить бумажные книги в каталог с минимальным ручным вводом;
- видеть, какие книги уже есть, и предотвращать покупку дублей;
- получать базовую статистику по коллекции (языки, жанры, полки);
- заложить основу для будущей работы с сериями и изданиями.

**MVP фокус**: удобный ввод + базовый учёт. Сложная аналитика серий — в v2.0.

### 1.2. Вне границ MVP

- Сложная серийная аналитика (полнота по языкам/линиям издания)
- Рекомендации и подборки
- Синхронизация/облако

## 2. Целевая аудитория

- Владельцы библиотек **100+ книг**
- Мультиязычные читатели (RU/EN/ZH/DE/ET/UK)
- Коллекционеры букинистики СССР
- Те, кто покупает дубли

## 3. Функциональные требования (FR)

### 3.1. Авторизация 

**FR-AUTH1. Регистрация/логин**
- Email + пароль
- Google Sign-In  

**FR-AUTH2. Локальное хранение**
- Guest mode (без синхронизации)
- Firebase Auth (в v2.0)

### 3.2. Добавление книги

**FR-1. Запуск добавления**  
- Главный экран: FAB ➕ "Добавить книгу"

**FR-2. Фото обложки**  
- Камера → предпросмотр → "Использовать фото"

**FR-3. Автозаполнение**  
- Предложить: Название / Автор / Язык / Жанр / Обложка

**FR-4. Редактирование** *(обязательные поля)*  
- ✅ Название  
- ✅ Автор(ы)  
- ✅ Язык  
- ✅ Комната  
- Жанр / Статус чтения / Стеллаж

Кнопка «Сохранить» недоступна, пока хотя бы одно из обязательных полей пустое.

#### FR-4.1. Поле «Язык»

- [MVP 0.1.0] Поле `Язык` реализовано как текстовое поле с автодополнением.
- [MVP 0.1.0] В БД хранится двухбуквенный код языка по ISO 639‑1 (например: `ru`, `en`, `zh`).
- [MVP 0.2.0] Источник подсказок:
    - встроенный справочник языков (ISO 639‑1) с autonyms (названиями языков на собственных языках);
    - ранее использованные языки (уникальные значения `language` из БД).
    - сначала отображаются использованные языки (по коду), затем остальные.
- [MVP 0.2.0] Формат подсказки: `Русский (ru)`, `Deutsch (de)`, `English (en)` и т.п.
- [MVP 0.2.0] Поведение выпадающего списка:
    - при первом открытии, если поле пустое, отображается полный список языков (сначала использованные);
    - при вводе текста список фильтруется по коду и названию (по подстроке, без учёта регистра);
    - при выборе язык сохраняется в БД как код, в поле отображается `Имя (код)`;
    - при повторном открытии при уже выбранном языке показывается полный список с галочкой у выбранного элемента;
    - нажатие «Сохранить» возможно только при непустом `selectedLanguageCode`.

#### FR-4.2. Поле «Комната *» (Location Level 1)

- [MVP 0.1.0] Поле «Комната» является обязательным.
- [MVP 0.1.0] Реализовано автодополнение:
    - список подсказок формируется из уникальных значений `locationLevel1` всех книг в БД;
    - подсказки фильтруются по введённому тексту без учёта регистра;
    - при выборе подсказки значение подставляется в поле и сохраняется в БД;
    - поле «Комната» является обязательным для сохранения записи;
    - кнопка «Сохранить» недоступна, пока поле «Комната» пустое.

**FR-5. Проверка дублей**  
- Поиск по Автор+Название (fuzzy)  
- Показать "Похожие: 2 книги" → объединить/новая?

**FR-6. Сохранение**  
- Книга в каталоге → возврат на главный экран

### 3.3. Каталог

**FR-7. Список книг**  
- Сетка: обложка / название / автор / язык / полка

**FR-8. Поиск**  
- По названию/автору (кириллица+латиница)

**FR-9. Фильтры**  
- Язык / Комната / Статус чтения

#### FR-9.1. Фильтрация по языку

- [MVP 0.1.0] На главном экране каталога над списком книг отображаются фильтры по языку в виде чипов:
    - первый чип — «All» (все языки);
    - далее — чипы языков, фактически встречающихся в библиотеке (уникальные значения поля `language` в БД).

- [MVP 0.1.0] Ограничение на количество фильтров:
    - панель языковых фильтров отображается только если количество различных языков от 1 до 10 включительно;
    - при количестве языков больше 10 панель языковых фильтров не отображается.

- [MVP 0.1.0] Логика работы фильтров:
    - при выборе «All» отображаются все книги;
    - при выборе конкретного языка отображаются только книги с соответствующим значением `language`;
    - выбранный фильтр визуально выделен (состояние «selected» у чипа).

- [MVP 0.1.0] Список языковых фильтров автоматически обновляется при изменении набора языков в библиотеке (добавление/удаление книг).

**FR-10. Карточка книги**  
- Детали + редактирование

- [MVP 0.3.0] При нажатии на книгу в каталоге открывается экран «Детали книги».
- [MVP 0.3.0] Экран «Детали книги» отображает:
  - название книги;
  - автора(ов);
  - язык (двухбуквенный код ISO 639‑1, как сохранён в БД);
  - комнату (location level 1).
- [MVP 0.3.0] На экране доступны действия:
  - «Назад» — возврат к каталогу;
  - «Редактировать» — открытие формы AddBook в режиме редактирования выбранной книги;
  - «Удалить» — открытие диалога подтверждения удаления.
- [MVP 0.3.0] При входе в режим редактирования поля «Название», «Автор(ы)», «Язык» и «Комната» предзаполнены данными выбранной книги; валидация и ограничения совпадают с режимом добавления новой книги.
- [MVP 0.3.0] При сохранении изменений в режиме редактирования обновляется существующая запись книги в БД, после чего пользователь возвращается на экран каталога с обновлёнными данными.
- [MVP 0.3.0] При удалении книги через диалог подтверждения запись удаляется из БД, диалог закрывается, пользователь возвращается в каталог, и удалённая книга не отображается в списке.
- [MVP 0.3.0] При отмене (кнопка «Назад» в AppBar или «Отмена» в диалоге удаления) изменения не сохраняются, книга не удаляется, пользователь остаётся на текущем/предыдущем экране.

### 3.4. Местоположение

**FR-11. Гибкая иерархия Location**  
- Level 1: Комната *(обязательно)*
- Level 2-5: Шкаф/Полка/Ряд/Позиция *(опционально)*

Примеры:  
- `Кабинет → Шкаф Классика → Полка 2 → Задний ряд → №15`
- `Гостиная → Полка 1 → Ряд 2 → (пусто) → (пусто)`

### 3.5. Статистика

**FR-13. Экран статистики**

| **Языки**     | **Кол-во** |
|---------------|------------|
| Русский       | 245        |
| Английский    | 87         |
| Китайский     | 23         |

| **Жанры**     | **Кол-во** |
|---------------|------------|
| Классика      | 112        |
| Фантастика    | 89         |
| Научпоп       | 67         |

| **Level 1 (Комнаты)** | **Кол-во** |
|-----------------------|------------|
| Кабинет               | 198        |
| Гостиная              | 45         |

| **Level 2 (Шкафы)**   | **Кол-во** |
|-----------------------|------------|
| Шкаф Классика         | 89         |
| Шкаф Фантастика       | 67         |

### 3.6. Экспорт

**FR-15. CSV экспорт**  
- Все книги → Downloads/bookshelf_export.csv

## 4. Нефункциональные требования (NFR)

### 4.1. Техстэк

- ✅ Android SDK 24+
- ✅ Kotlin + Jetpack Compose (Material 3)
- ✅ Room SQLite (FTS5 fuzzy search)
- ✅ MVVM + Coroutines/Flow
- ✅ Google Lens Intent API (MVP stub OK)

### 4.2. Производительность
- Поиск 10k книг < 1с  
- Открытие каталога мгновенно

### 4.3. UX
- **Добавление книги: 3-5 тапов**  
- Русский UI + тёмная тема
- Крупный шрифт (доступность)

### 4.4. Оффлайн
- ✅ Полностью оффлайн (кроме Google Lens)

## 5. Приоритизация MVP

🔥 MUST HAVE (Core):
- FR-1…FR-6 (Добавление + дубли)
- FR-7…FR-10 (Каталог + поиск)
- FR-11 (Комната)
- FR-13 (Статистика)

⚠️ SHOULD HAVE:
- FR-12 (Полная иерархия location)
- FR-15 (CSV экспорт)

🔄 COULD HAVE:
- Google Lens stub

## 6. User Stories

- Как коллекционер, хочу сфоткать книгу → за 3 клика она в каталоге
- Как владелец библиотеки, хочу фильтр по полке → найти книгу мгновенно
- Как мультиязычный читатель, хочу статистику языков → понимать состав коллекции

## 7. Метрики успеха

- 🎯 Время инвентаризации 100 книг: <30 мин
- 🎯 Поиск по 10k книг: <1с
- 🎯 Добавление книги: 3-5 тапов
